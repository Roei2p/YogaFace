services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./server/data:/app/data
    restart: unless-stopped

  web:
    build: ./web
    ports:
      - "8080:80"
    depends_on:
      - server
    restart: unless-stopped
